/**
 * FancyMumble profile customisation embedded in the Mumble user comment.
 *
 * Defined in the shared profile-card package so the client and the standalone
 * channel viewer agree on the record byte for byte.
 */
export * from "@shared/profilecard/profileTypes";
