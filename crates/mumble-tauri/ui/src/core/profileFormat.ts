/**
 * FancyMumble profile format - serialisation helpers.
 *
 * Shared with the standalone channel viewer, which reads the same marker out
 * of the same Mumble comments; this module is the client's import path for it.
 */
export * from "@shared/profilecard/profileFormat";
