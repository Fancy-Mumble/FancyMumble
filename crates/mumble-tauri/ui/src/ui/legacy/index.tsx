import "./global.css";
import { initializeLegacyAppearance } from "./appearance";

initializeLegacyAppearance();

/** The existing interface remains an independent, supported UI pack. */
export { default } from "./App";
